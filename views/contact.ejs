<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= title %></title>
  </head>
  <body>
<div class="container">
  <div class="row-fluid">
    <% if(typeof msg !== 'undefined') { %>
      <% if(!!err) { %>
        <div class="msgbox err"></div>
      <% } else { %>
        <div class="msgbox success"></div>
    <% } %>
  </div>
</div>

</body>
</html>
